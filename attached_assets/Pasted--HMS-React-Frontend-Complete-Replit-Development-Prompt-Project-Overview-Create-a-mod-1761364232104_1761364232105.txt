# 🏥 HMS React Frontend - Complete Replit Development Prompt

## 🎯 Project Overview

Create a **modern, production-ready React frontend** for the Hospital Management System (HMS) that integrates seamlessly with the existing Django REST API backend. This is a comprehensive healthcare management platform with 14 implemented modules and 300+ API endpoints.

## 📋 Project Requirements

### **Technology Stack**
```json
{
  "framework": "React 18.2+",
  "language": "TypeScript",
  "stateManagement": "Redux Toolkit",
  "routing": "React Router v6",
  "uiLibrary": "Material-UI (MUI) v5",
  "formHandling": "React Hook Form + Yup",
  "apiClient": "Axios",
  "charts": "Recharts",
  "dateHandling": "date-fns",
  "notifications": "react-toastify",
  "authentication": "JWT (access + refresh tokens)",
  "styling": "Emotion (CSS-in-JS)"
}
```

### **Backend API Base URL**
```
Development: http://localhost:8000/api/
Production: https://your-backend-domain.com/api/
```

## 🏗️ Project Structure

```
hms-frontend/
├── public/
│   ├── index.html
│   ├── favicon.ico
│   └── assets/
├── src/
│   ├── api/                      # API integration layer
│   │   ├── axios.config.ts       # Axios configuration
│   │   ├── auth.api.ts           # Authentication APIs
│   │   ├── patients.api.ts       # Patient APIs
│   │   ├── appointments.api.ts   # Appointment APIs
│   │   ├── encounters.api.ts     # Clinical encounter APIs
│   │   ├── billing.api.ts        # Billing APIs
│   │   ├── laboratory.api.ts     # Laboratory APIs
│   │   ├── inventory.api.ts      # Inventory APIs
│   │   ├── telemedicine.api.ts   # Telemedicine APIs
│   │   ├── analytics.api.ts      # Analytics APIs
│   │   └── index.ts              # API exports
│   ├── components/               # Reusable components
│   │   ├── common/               # Common UI components
│   │   │   ├── Navbar.tsx
│   │   │   ├── Sidebar.tsx
│   │   │   ├── Footer.tsx
│   │   │   ├── LoadingSpinner.tsx
│   │   │   ├── ErrorBoundary.tsx
│   │   │   ├── ConfirmDialog.tsx
│   │   │   ├── SearchBar.tsx
│   │   │   └── DataTable.tsx
│   │   ├── patients/             # Patient components
│   │   │   ├── PatientList.tsx
│   │   │   ├── PatientForm.tsx
│   │   │   ├── PatientDetails.tsx
│   │   │   ├── PatientSearch.tsx
│   │   │   └── PatientCard.tsx
│   │   ├── appointments/         # Appointment components
│   │   │   ├── AppointmentList.tsx
│   │   │   ├── AppointmentForm.tsx
│   │   │   ├── AppointmentCalendar.tsx
│   │   │   ├── DoctorSchedule.tsx
│   │   │   └── AppointmentCard.tsx
│   │   ├── encounters/           # Clinical encounter components
│   │   │   ├── EncounterList.tsx
│   │   │   ├── EncounterForm.tsx
│   │   │   ├── ClinicalNotes.tsx
│   │   │   ├── VitalsForm.tsx
│   │   │   └── PrescriptionForm.tsx
│   │   ├── billing/              # Billing components
│   │   │   ├── InvoiceList.tsx
│   │   │   ├── InvoiceForm.tsx
│   │   │   ├── PaymentForm.tsx
│   │   │   └── PaymentHistory.tsx
│   │   ├── laboratory/           # Laboratory components
│   │   │   ├── LabTestList.tsx
│   │   │   ├── LabOrderForm.tsx
│   │   │   ├── LabResultsView.tsx
│   │   │   └── LabReports.tsx
│   │   ├── inventory/            # Inventory components
│   │   │   ├── MedicineList.tsx
│   │   │   ├── StockManagement.tsx
│   │   │   ├── PurchaseOrderForm.tsx
│   │   │   └── StockAlerts.tsx
│   │   ├── analytics/            # Analytics components
│   │   │   ├── Dashboard.tsx
│   │   │   ├── PatientAnalytics.tsx
│   │   │   ├── RevenueChart.tsx
│   │   │   └── PerformanceMetrics.tsx
│   │   └── telemedicine/         # Telemedicine components
│   │       ├── SessionList.tsx
│   │       ├── VideoCall.tsx
│   │       └── RemoteMonitoring.tsx
│   ├── pages/                    # Page components
│   │   ├── auth/
│   │   │   ├── Login.tsx
│   │   │   ├── Register.tsx
│   │   │   ├── ForgotPassword.tsx
│   │   │   └── ResetPassword.tsx
│   │   ├── dashboard/
│   │   │   ├── Dashboard.tsx
│   │   │   └── AdminDashboard.tsx
│   │   ├── patients/
│   │   │   ├── PatientsPage.tsx
│   │   │   ├── PatientDetailsPage.tsx
│   │   │   └── AddPatientPage.tsx
│   │   ├── appointments/
│   │   │   ├── AppointmentsPage.tsx
│   │   │   ├── BookAppointmentPage.tsx
│   │   │   └── AppointmentDetailsPage.tsx
│   │   ├── encounters/
│   │   │   ├── EncountersPage.tsx
│   │   │   └── EncounterDetailsPage.tsx
│   │   ├── billing/
│   │   │   ├── BillingPage.tsx
│   │   │   └── InvoiceDetailsPage.tsx
│   │   ├── laboratory/
│   │   │   ├── LaboratoryPage.tsx
│   │   │   └── LabResultsPage.tsx
│   │   ├── inventory/
│   │   │   ├── InventoryPage.tsx
│   │   │   └── MedicineDetailsPage.tsx
│   │   ├── analytics/
│   │   │   └── AnalyticsPage.tsx
│   │   └── settings/
│   │       ├── ProfilePage.tsx
│   │       └── SettingsPage.tsx
│   ├── store/                    # Redux store
│   │   ├── index.ts              # Store configuration
│   │   ├── slices/
│   │   │   ├── authSlice.ts
│   │   │   ├── patientsSlice.ts
│   │   │   ├── appointmentsSlice.ts
│   │   │   ├── encountersSlice.ts
│   │   │   ├── billingSlice.ts
│   │   │   ├── laboratorySlice.ts
│   │   │   ├── inventorySlice.ts
│   │   │   └── uiSlice.ts
│   │   └── hooks.ts              # Typed hooks
│   ├── types/                    # TypeScript types
│   │   ├── auth.types.ts
│   │   ├── patient.types.ts
│   │   ├── appointment.types.ts
│   │   ├── encounter.types.ts
│   │   ├── billing.types.ts
│   │   ├── laboratory.types.ts
│   │   ├── inventory.types.ts
│   │   └── common.types.ts
│   ├── utils/                    # Utility functions
│   │   ├── formatters.ts         # Date, currency formatters
│   │   ├── validators.ts         # Form validators
│   │   ├── constants.ts          # App constants
│   │   ├── helpers.ts            # Helper functions
│   │   └── storage.ts            # LocalStorage helpers
│   ├── hooks/                    # Custom hooks
│   │   ├── useAuth.ts
│   │   ├── usePatients.ts
│   │   ├── useAppointments.ts
│   │   └── useDebounce.ts
│   ├── routes/                   # Routing configuration
│   │   ├── AppRoutes.tsx
│   │   ├── PrivateRoute.tsx
│   │   └── PublicRoute.tsx
│   ├── theme/                    # MUI theme configuration
│   │   ├── theme.ts
│   │   └── colors.ts
│   ├── App.tsx                   # Main App component
│   ├── index.tsx                 # Entry point
│   └── setupTests.ts             # Test configuration
├── .env.development              # Development environment
├── .env.production               # Production environment
├── .gitignore
├── package.json
├── tsconfig.json
└── README.md
```

## 🔐 Authentication Implementation

### **1. JWT Token Management**

```typescript
// src/api/axios.config.ts
import axios from 'axios';

const API_BASE_URL = process.env.REACT_APP_API_URL || 'http://localhost:8000/api';

const axiosInstance = axios.create({
  baseURL: API_BASE_URL,
  headers: {
    'Content-Type': 'application/json',
  },
});

// Request interceptor - Add JWT token
axiosInstance.interceptors.request.use(
  (config) => {
    const token = localStorage.getItem('access_token');
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  },
  (error) => Promise.reject(error)
);

// Response interceptor - Handle token refresh
axiosInstance.interceptors.response.use(
  (response) => response,
  async (error) => {
    const originalRequest = error.config;

    // If 401 and not already retried, try to refresh token
    if (error.response?.status === 401 && !originalRequest._retry) {
      originalRequest._retry = true;

      try {
        const refreshToken = localStorage.getItem('refresh_token');
        const response = await axios.post(`${API_BASE_URL}/auth/refresh/`, {
          refresh: refreshToken,
        });

        const { access } = response.data;
        localStorage.setItem('access_token', access);

        originalRequest.headers.Authorization = `Bearer ${access}`;
        return axiosInstance(originalRequest);
      } catch (refreshError) {
        // Refresh failed, logout user
        localStorage.removeItem('access_token');
        localStorage.removeItem('refresh_token');
        window.location.href = '/login';
        return Promise.reject(refreshError);
      }
    }

    return Promise.reject(error);
  }
);

export default axiosInstance;
```

### **2. Authentication API**

```typescript
// src/api/auth.api.ts
import axiosInstance from './axios.config';

export interface LoginCredentials {
  username: string;
  password: string;
}

export interface AuthResponse {
  access: string;
  refresh: string;
  user?: {
    id: string;
    username: string;
    email: string;
    first_name: string;
    last_name: string;
  };
}

export const authAPI = {
  login: async (credentials: LoginCredentials): Promise<AuthResponse> => {
    const response = await axiosInstance.post('/auth/login/', credentials);
    return response.data;
  },

  logout: async (): Promise<void> => {
    await axiosInstance.post('/auth/logout/');
  },

  refreshToken: async (refreshToken: string): Promise<{ access: string }> => {
    const response = await axiosInstance.post('/auth/refresh/', {
      refresh: refreshToken,
    });
    return response.data;
  },

  getSession: async () => {
    const response = await axiosInstance.get('/auth/session/');
    return response.data;
  },

  changePassword: async (data: {
    current_password: string;
    new_password: string;
  }) => {
    const response = await axiosInstance.post('/auth/change-password/', data);
    return response.data;
  },
};
```

### **3. Auth Redux Slice**

```typescript
// src/store/slices/authSlice.ts
import { createSlice, createAsyncThunk, PayloadAction } from '@reduxjs/toolkit';
import { authAPI, LoginCredentials, AuthResponse } from '../../api/auth.api';

interface AuthState {
  user: any | null;
  accessToken: string | null;
  refreshToken: string | null;
  isAuthenticated: boolean;
  loading: boolean;
  error: string | null;
}

const initialState: AuthState = {
  user: null,
  accessToken: localStorage.getItem('access_token'),
  refreshToken: localStorage.getItem('refresh_token'),
  isAuthenticated: !!localStorage.getItem('access_token'),
  loading: false,
  error: null,
};

export const login = createAsyncThunk(
  'auth/login',
  async (credentials: LoginCredentials, { rejectWithValue }) => {
    try {
      const response = await authAPI.login(credentials);
      localStorage.setItem('access_token', response.access);
      localStorage.setItem('refresh_token', response.refresh);
      return response;
    } catch (error: any) {
      return rejectWithValue(error.response?.data?.message || 'Login failed');
    }
  }
);

export const logout = createAsyncThunk('auth/logout', async () => {
  await authAPI.logout();
  localStorage.removeItem('access_token');
  localStorage.removeItem('refresh_token');
});

const authSlice = createSlice({
  name: 'auth',
  initialState,
  reducers: {
    clearError: (state) => {
      state.error = null;
    },
  },
  extraReducers: (builder) => {
    builder
      .addCase(login.pending, (state) => {
        state.loading = true;
        state.error = null;
      })
      .addCase(login.fulfilled, (state, action: PayloadAction<AuthResponse>) => {
        state.loading = false;
        state.isAuthenticated = true;
        state.accessToken = action.payload.access;
        state.refreshToken = action.payload.refresh;
        state.user = action.payload.user || null;
      })
      .addCase(login.rejected, (state, action) => {
        state.loading = false;
        state.error = action.payload as string;
      })
      .addCase(logout.fulfilled, (state) => {
        state.user = null;
        state.accessToken = null;
        state.refreshToken = null;
        state.isAuthenticated = false;
      });
  },
});

export const { clearError } = authSlice.actions;
export default authSlice.reducer;
```

## 📦 Core Module Implementations

### **1. Patient Management Module**

```typescript
// src/api/patients.api.ts
import axiosInstance from './axios.config';
import { Patient, PatientCreateData, PatientSearchParams } from '../types/patient.types';

export const patientsAPI = {
  // List patients with pagination
  getPatients: async (params?: {
    page?: number;
    page_size?: number;
    search?: string;
    ordering?: string;
  }) => {
    const response = await axiosInstance.get('/patients/patients/', { params });
    return response.data;
  },

  // Get single patient
  getPatient: async (id: string): Promise<Patient> => {
    const response = await axiosInstance.get(`/patients/patients/${id}/`);
    return response.data;
  },

  // Create patient
  createPatient: async (data: PatientCreateData): Promise<Patient> => {
    const response = await axiosInstance.post('/patients/patients/', data);
    return response.data;
  },

  // Update patient
  updatePatient: async (id: string, data: Partial<PatientCreateData>): Promise<Patient> => {
    const response = await axiosInstance.put(`/patients/patients/${id}/`, data);
    return response.data;
  },

  // Delete patient
  deletePatient: async (id: string): Promise<void> => {
    await axiosInstance.delete(`/patients/patients/${id}/`);
  },

  // Search patients
  searchPatients: async (params: PatientSearchParams) => {
    const response = await axiosInstance.get('/patients/search/', { params });
    return response.data;
  },

  // Get patient medical history
  getPatientMedicalHistory: async (id: string) => {
    const response = await axiosInstance.get(`/patients/${id}/medical-history/`);
    return response.data;
  },

  // Get patient conditions
  getPatientConditions: async (patientId: string) => {
    const response = await axiosInstance.get('/patients/conditions/', {
      params: { patient: patientId },
    });
    return response.data;
  },

  // Get patient allergies
  getPatientAllergies: async (patientId: string) => {
    const response = await axiosInstance.get('/patients/allergies/', {
      params: { patient: patientId },
    });
    return response.data;
  },

  // Export patients
  exportPatients: async (format: 'csv' | 'excel', filters?: any) => {
    const response = await axiosInstance.get('/patients/export/', {
      params: { format, ...filters },
      responseType: 'blob',
    });
    return response.data;
  },
};
```

```typescript
// src/types/patient.types.ts
export interface Patient {
  id: string;
  patient_code: string;
  full_name: string;
  date_of_birth: string;
  gender: 'M' | 'F' | 'O';
  phone: string;
  email: string;
  address: string;
  city: string;
  state: string;
  pin_code: string;
  emergency_contact_name?: string;
  emergency_contact_phone?: string;
  emergency_contact_relation?: string;
  insurance_provider?: string;
  insurance_policy_number?: string;
  insurance_valid_until?: string;
  is_active: boolean;
  created_at: string;
  updated_at?: string;
}

export interface PatientCreateData {
  full_name: string;
  date_of_birth: string;
  gender: 'M' | 'F' | 'O';
  phone: string;
  email?: string;
  address?: string;
  city?: string;
  state?: string;
  pin_code?: string;
  emergency_contact_name?: string;
  emergency_contact_phone?: string;
  emergency_contact_relation?: string;
}

export interface PatientSearchParams {
  q?: string;
  filters?: string;
  page?: number;
  page_size?: number;
}
```

### **2. Appointment Management Module**

```typescript
// src/api/appointments.api.ts
import axiosInstance from './axios.config';
import { Appointment, AppointmentCreateData } from '../types/appointment.types';

export const appointmentsAPI = {
  // List appointments
  getAppointments: async (params?: {
    page?: number;
    page_size?: number;
    status?: string;
    patient?: string;
    doctor?: string;
    appointment_date?: string;
  }) => {
    const response = await axiosInstance.get('/appointments/appointments/', { params });
    return response.data;
  },

  // Get single appointment
  getAppointment: async (id: string): Promise<Appointment> => {
    const response = await axiosInstance.get(`/appointments/appointments/${id}/`);
    return response.data;
  },

  // Create appointment
  createAppointment: async (data: AppointmentCreateData): Promise<Appointment> => {
    const response = await axiosInstance.post('/appointments/appointments/', data);
    return response.data;
  },

  // Update appointment
  updateAppointment: async (id: string, data: Partial<AppointmentCreateData>): Promise<Appointment> => {
    const response = await axiosInstance.put(`/appointments/appointments/${id}/`, data);
    return response.data;
  },

  // Delete appointment
  deleteAppointment: async (id: string): Promise<void> => {
    await axiosInstance.delete(`/appointments/appointments/${id}/`);
  },

  // Check doctor availability
  checkAvailability: async (params: {
    doctor: string;
    date: string;
    duration?: number;
  }) => {
    const response = await axiosInstance.get('/appointments/availability/', { params });
    return response.data;
  },

  // Reschedule appointment
  rescheduleAppointment: async (data: {
    appointment: string;
    new_date: string;
    new_time: string;
    reason?: string;
  }) => {
    const response = await axiosInstance.post('/appointments/reschedule/', data);
    return response.data;
  },

  // Get appointment analytics
  getAppointmentAnalytics: async (params?: {
    period?: string;
    doctor?: string;
  }) => {
    const response = await axiosInstance.get('/appointments/analytics/', { params });
    return response.data;
  },

  // Send appointment reminders
  sendReminder: async (data: {
    appointment: string;
    reminder_type: 'sms' | 'email';
    reminder_time: string;
  }) => {
    const response = await axiosInstance.post('/appointments/reminders/', data);
    return response.data;
  },
};
```

### **3. Clinical Encounters Module**

```typescript
// src/api/encounters.api.ts
import axiosInstance from './axios.config';
import { Encounter, EncounterCreateData, ClinicalNote, Prescription } from '../types/encounter.types';

export const encountersAPI = {
  // List encounters
  getEncounters: async (params?: {
    page?: number;
    page_size?: number;
    patient?: string;
    doctor?: string;
    encounter_type?: string;
  }) => {
    const response = await axiosInstance.get('/encounters/encounters/', { params });
    return response.data;
  },

  // Get single encounter
  getEncounter: async (id: string): Promise<Encounter> => {
    const response = await axiosInstance.get(`/encounters/encounters/${id}/`);
    return response.data;
  },

  // Create encounter
  createEncounter: async (data: EncounterCreateData): Promise<Encounter> => {
    const response = await axiosInstance.post('/encounters/encounters/', data);
    return response.data;
  },

  // Update encounter
  updateEncounter: async (id: string, data: Partial<EncounterCreateData>): Promise<Encounter> => {
    const response = await axiosInstance.put(`/encounters/encounters/${id}/`, data);
    return response.data;
  },

  // Clinical Notes
  getClinicalNotes: async (encounterId: string) => {
    const response = await axiosInstance.get('/encounters/clinical-notes/', {
      params: { encounter: encounterId },
    });
    return response.data;
  },

  createClinicalNote: async (data: ClinicalNote) => {
    const response = await axiosInstance.post('/encounters/clinical-notes/', data);
    return response.data;
  },

  // Prescriptions
  getPrescriptions: async (encounterId: string) => {
    const response = await axiosInstance.get('/encounters/prescriptions/', {
      params: { encounter: encounterId },
    });
    return response.data;
  },

  createPrescription: async (data: Prescription) => {
    const response = await axiosInstance.post('/encounters/prescriptions/', data);
    return response.data;
  },
};
```

### **4. Billing Module**

```typescript
// src/api/billing.api.ts
import axiosInstance from './axios.config';
import { Invoice, Payment, InvoiceCreateData } from '../types/billing.types';

export const billingAPI = {
  // Invoices
  getInvoices: async (params?: {
    page?: number;
    page_size?: number;
    patient?: string;
    status?: string;
  }) => {
    const response = await axiosInstance.get('/billing/invoices/', { params });
    return response.data;
  },

  getInvoice: async (id: string): Promise<Invoice> => {
    const response = await axiosInstance.get(`/billing/invoices/${id}/`);
    return response.data;
  },

  createInvoice: async (data: InvoiceCreateData): Promise<Invoice> => {
    const response = await axiosInstance.post('/billing/invoices/', data);
    return response.data;
  },

  updateInvoice: async (id: string, data: Partial<InvoiceCreateData>): Promise<Invoice> => {
    const response = await axiosInstance.put(`/billing/invoices/${id}/`, data);
    return response.data;
  },

  // Payments
  getPayments: async (params?: { invoice?: string }) => {
    const response = await axiosInstance.get('/billing/payments/', { params });
    return response.data;
  },

  createPayment: async (data: Payment) => {
    const response = await axiosInstance.post('/billing/payments/', data);
    return response.data;
  },

  // Revenue Analytics
  getRevenueAnalysis: async (params?: {
    period?: string;
    department?: string;
  }) => {
    const response = await axiosInstance.get('/billing/revenue-analysis/', { params });
    return response.data;
  },
};
```

### **5. Laboratory Module**

```typescript
// src/api/laboratory.api.ts
import axiosInstance from './axios.config';

export const laboratoryAPI = {
  // Lab Tests
  getLabTests: async (params?: { category?: string }) => {
    const response = await axiosInstance.get('/laboratory/tests/', { params });
    return response.data;
  },

  // Lab Orders
  getLabOrders: async (params?: { patient?: string; status?: string }) => {
    const response = await axiosInstance.get('/laboratory/orders/', { params });
    return response.data;
  },

  createLabOrder: async (data: any) => {
    const response = await axiosInstance.post('/laboratory/orders/', data);
    return response.data;
  },

  // Lab Results
  getLabResults: async (orderId: string) => {
    const response = await axiosInstance.get('/laboratory/results/', {
      params: { order: orderId },
    });
    return response.data;
  },

  createLabResult: async (data: any) => {
    const response = await axiosInstance.post('/laboratory/results/', data);
    return response.data;
  },
};
```

### **6. Analytics Module**

```typescript
// src/api/analytics.api.ts
import axiosInstance from './axios.config';

export const analyticsAPI = {
  // Dashboard Overview
  getDashboardOverview: async () => {
    const response = await axiosInstance.get('/dashboard/overview/');
    return response.data;
  },

  // Patient Analytics
  getPatientAnalytics: async (params?: { period?: string }) => {
    const response = await axiosInstance.get('/analytics/patient-analytics/', { params });
    return response.data;
  },

  // Clinic Analytics
  getClinicAnalytics: async (params?: { period?: string }) => {
    const response = await axiosInstance.get('/analytics/clinic-analytics/', { params });
    return response.data;
  },

  // Revenue Analytics
  getRevenueAnalytics: async (params?: { period?: string }) => {
    const response = await axiosInstance.get('/analytics/revenue-analytics/', { params });
    return response.data;
  },

  // Real-time Dashboard
  getRealTimeDashboard: async () => {
    const response = await axiosInstance.get('/dashboard/real-time/');
    return response.data;
  },
};
```

## 🎨 UI Components

### **1. Dashboard Component**

```typescript
// src/pages/dashboard/Dashboard.tsx
import React, { useEffect, useState } from 'react';
import {
  Box,
  Grid,
  Card,
  CardContent,
  Typography,
  CircularProgress,
} from '@mui/material';
import {
  People as PeopleIcon,
  EventNote as EventNoteIcon,
  AttachMoney as MoneyIcon,
  LocalHospital as HospitalIcon,
} from '@mui/icons-material';
import { analyticsAPI } from '../../api/analytics.api';

interface DashboardMetrics {
  total_patients: number;
  active_appointments: number;
  pending_tasks: number;
  revenue_today: number;
}

const Dashboard: React.FC = () => {
  const [metrics, setMetrics] = useState<DashboardMetrics | null>(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    fetchDashboardData();
  }, []);

  const fetchDashboardData = async () => {
    try {
      const data = await analyticsAPI.getDashboardOverview();
      setMetrics(data.metrics);
    } catch (error) {
      console.error('Failed to fetch dashboard data:', error);
    } finally {
      setLoading(false);
    }
  };

  if (loading) {
    return (
      <Box display="flex" justifyContent="center" alignItems="center" minHeight="400px">
        <CircularProgress />
      </Box>
    );
  }

  const cards = [
    {
      title: 'Total Patients',
      value: metrics?.total_patients || 0,
      icon: <PeopleIcon fontSize="large" />,
      color: '#1976d2',
    },
    {
      title: 'Active Appointments',
      value: metrics?.active_appointments || 0,
      icon: <EventNoteIcon fontSize="large" />,
      color: '#2e7d32',
    },
    {
      title: 'Pending Tasks',
      value: metrics?.pending_tasks || 0,
      icon: <HospitalIcon fontSize="large" />,
      color: '#ed6c02',
    },
    {
      title: "Today's Revenue",
      value: `₹${metrics?.revenue_today?.toLocaleString() || 0}`,
      icon: <MoneyIcon fontSize="large" />,
      color: '#9c27b0',
    },
  ];

  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" gutterBottom>
        Dashboard
      </Typography>

      <Grid container spacing={3}>
        {cards.map((card, index) => (
          <Grid item xs={12} sm={6} md={3} key={index}>
            <Card
              sx={{
                height: '100%',
                background: `linear-gradient(135deg, ${card.color} 0%, ${card.color}dd 100%)`,
                color: 'white',
              }}
            >
              <CardContent>
                <Box display="flex" justifyContent="space-between" alignItems="center">
                  <Box>
                    <Typography variant="h6" gutterBottom>
                      {card.title}
                    </Typography>
                    <Typography variant="h4">{card.value}</Typography>
                  </Box>
                  <Box>{card.icon}</Box>
                </Box>
              </CardContent>
            </Card>
          </Grid>
        ))}
      </Grid>
    </Box>
  );
};

export default Dashboard;
```

### **2. Patient List Component**

```typescript
// src/components/patients/PatientList.tsx
import React, { useEffect, useState } from 'react';
import {
  Box,
  Button,
  Paper,
  Table,
  TableBody,
  TableCell,
  TableContainer,
  TableHead,
  TableRow,
  TablePagination,
  TextField,
  IconButton,
  Chip,
} from '@mui/material';
import {
  Edit as EditIcon,
  Delete as DeleteIcon,
  Visibility as ViewIcon,
  Add as AddIcon,
} from '@mui/icons-material';
import { useNavigate } from 'react-router-dom';
import { patientsAPI } from '../../api/patients.api';
import { Patient } from '../../types/patient.types';

const PatientList: React.FC = () => {
  const navigate = useNavigate();
  const [patients, setPatients] = useState<Patient[]>([]);
  const [loading, setLoading] = useState(true);
  const [page, setPage] = useState(0);
  const [rowsPerPage, setRowsPerPage] = useState(10);
  const [totalCount, setTotalCount] = useState(0);
  const [searchQuery, setSearchQuery] = useState('');

  useEffect(() => {
    fetchPatients();
  }, [page, rowsPerPage, searchQuery]);

  const fetchPatients = async () => {
    setLoading(true);
    try {
      const data = await patientsAPI.getPatients({
        page: page + 1,
        page_size: rowsPerPage,
        search: searchQuery,
      });
      setPatients(data.results);
      setTotalCount(data.count);
    } catch (error) {
      console.error('Failed to fetch patients:', error);
    } finally {
      setLoading(false);
    }
  };

  const handleChangePage = (event: unknown, newPage: number) => {
    setPage(newPage);
  };

  const handleChangeRowsPerPage = (event: React.ChangeEvent<HTMLInputElement>) => {
    setRowsPerPage(parseInt(event.target.value, 10));
    setPage(0);
  };

  const handleSearch = (event: React.ChangeEvent<HTMLInputElement>) => {
    setSearchQuery(event.target.value);
    setPage(0);
  };

  const handleDelete = async (id: string) => {
    if (window.confirm('Are you sure you want to delete this patient?')) {
      try {
        await patientsAPI.deletePatient(id);
        fetchPatients();
      } catch (error) {
        console.error('Failed to delete patient:', error);
      }
    }
  };

  return (
    <Box sx={{ p: 3 }}>
      <Box display="flex" justifyContent="space-between" alignItems="center" mb={3}>
        <TextField
          label="Search Patients"
          variant="outlined"
          size="small"
          value={searchQuery}
          onChange={handleSearch}
          sx={{ width: 300 }}
        />
        <Button
          variant="contained"
          startIcon={<AddIcon />}
          onClick={() => navigate('/patients/add')}
        >
          Add Patient
        </Button>
      </Box>

      <TableContainer component={Paper}>
        <Table>
          <TableHead>
            <TableRow>
              <TableCell>Patient Code</TableCell>
              <TableCell>Name</TableCell>
              <TableCell>Gender</TableCell>
              <TableCell>Phone</TableCell>
              <TableCell>Email</TableCell>
              <TableCell>Status</TableCell>
              <TableCell align="center">Actions</TableCell>
            </TableRow>
          </TableHead>
          <TableBody>
            {patients.map((patient) => (
              <TableRow key={patient.id}>
                <TableCell>{patient.patient_code}</TableCell>
                <TableCell>{patient.full_name}</TableCell>
                <TableCell>{patient.gender}</TableCell>
                <TableCell>{patient.phone}</TableCell>
                <TableCell>{patient.email || '-'}</TableCell>
                <TableCell>
                  <Chip
                    label={patient.is_active ? 'Active' : 'Inactive'}
                    color={patient.is_active ? 'success' : 'default'}
                    size="small"
                  />
                </TableCell>
                <TableCell align="center">
                  <IconButton
                    size="small"
                    onClick={() => navigate(`/patients/${patient.id}`)}
                  >
                    <ViewIcon />
                  </IconButton>
                  <IconButton
                    size="small"
                    onClick={() => navigate(`/patients/${patient.id}/edit`)}
                  >
                    <EditIcon />
                  </IconButton>
                  <IconButton
                    size="small"
                    color="error"
                    onClick={() => handleDelete(patient.id)}
                  >
                    <DeleteIcon />
                  </IconButton>
                </TableCell>
              </TableRow>
            ))}
          </TableBody>
        </Table>
        <TablePagination
          component="div"
          count={totalCount}
          page={page}
          onPageChange={handleChangePage}
          rowsPerPage={rowsPerPage}
          onRowsPerPageChange={handleChangeRowsPerPage}
        />
      </TableContainer>
    </Box>
  );
};

export default PatientList;
```

## 🚀 Getting Started

### **Step 1: Initialize Project**

```bash
# Create React app with TypeScript
npx create-react-app hms-frontend --template typescript

# Navigate to project
cd hms-frontend

# Install dependencies
npm install @mui/material @emotion/react @emotion/styled
npm install @reduxjs/toolkit react-redux
npm install react-router-dom
npm install axios
npm install react-hook-form yup @hookform/resolvers
npm install recharts
npm install date-fns
npm install react-toastify
npm install @mui/icons-material
npm install @mui/x-date-pickers
```

### **Step 2: Environment Configuration**

```bash
# Create .env.development
REACT_APP_API_URL=http://localhost:8000/api
REACT_APP_ENV=development

# Create .env.production
REACT_APP_API_URL=https://your-backend-domain.com/api
REACT_APP_ENV=production
```

### **Step 3: Configure Redux Store**

```typescript
// src/store/index.ts
import { configureStore } from '@reduxjs/toolkit';
import authReducer from './slices/authSlice';
import patientsReducer from './slices/patientsSlice';
import appointmentsReducer from './slices/appointmentsSlice';
import encountersReducer from './slices/encountersSlice';
import billingReducer from './slices/billingSlice';
import uiReducer from './slices/uiSlice';

export const store = configureStore({
  reducer: {
    auth: authReducer,
    patients: patientsReducer,
    appointments: appointmentsReducer,
    encounters: encountersReducer,
    billing: billingReducer,
    ui: uiReducer,
  },
});

export type RootState = ReturnType<typeof store.getState>;
export type AppDispatch = typeof store.dispatch;
```

### **Step 4: Configure Routes**

```typescript
// src/routes/AppRoutes.tsx
import React from 'react';
import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom';
import PrivateRoute from './PrivateRoute';
import Login from '../pages/auth/Login';
import Dashboard from '../pages/dashboard/Dashboard';
import PatientsPage from '../pages/patients/PatientsPage';
import AppointmentsPage from '../pages/appointments/AppointmentsPage';
import EncountersPage from '../pages/encounters/EncountersPage';
import BillingPage from '../pages/billing/BillingPage';
import LaboratoryPage from '../pages/laboratory/LaboratoryPage';
import InventoryPage from '../pages/inventory/InventoryPage';
import AnalyticsPage from '../pages/analytics/AnalyticsPage';

const AppRoutes: React.FC = () => {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/login" element={<Login />} />
        
        <Route path="/" element={<PrivateRoute />}>
          <Route path="/" element={<Navigate to="/dashboard" replace />} />
          <Route path="/dashboard" element={<Dashboard />} />
          <Route path="/patients/*" element={<PatientsPage />} />
          <Route path="/appointments/*" element={<AppointmentsPage />} />
          <Route path="/encounters/*" element={<EncountersPage />} />
          <Route path="/billing/*" element={<BillingPage />} />
          <Route path="/laboratory/*" element={<LaboratoryPage />} />
          <Route path="/inventory/*" element={<InventoryPage />} />
          <Route path="/analytics" element={<AnalyticsPage />} />
        </Route>
      </Routes>
    </BrowserRouter>
  );
};

export default AppRoutes;
```

## 📋 Implementation Checklist

### **Phase 1: Core Setup (Week 1)**
- [ ] Initialize React TypeScript project
- [ ] Install all dependencies
- [ ] Configure environment variables
- [ ] Setup Redux store with auth slice
- [ ] Implement Axios configuration with interceptors
- [ ] Create authentication API integration
- [ ] Build login/logout functionality
- [ ] Implement JWT token refresh mechanism
- [ ] Create private route wrapper
- [ ] Setup MUI theme configuration

### **Phase 2: Patient Management (Week 2)**
- [ ] Create patient types and interfaces
- [ ] Implement patients API integration
- [ ] Build patient list component with pagination
- [ ] Create patient form (add/edit)
- [ ] Implement patient search functionality
- [ ] Build patient details page
- [ ] Add patient medical history view
- [ ] Implement patient conditions management
- [ ] Add patient allergies management
- [ ] Create patient export functionality

### **Phase 3: Appointment Management (Week 3)**
- [ ] Create appointment types and interfaces
- [ ] Implement appointments API integration
- [ ] Build appointment list component
- [ ] Create appointment booking form
- [ ] Implement doctor availability checker
- [ ] Build appointment calendar view
- [ ] Add appointment rescheduling
- [ ] Implement appointment reminders
- [ ] Create appointment analytics dashboard
- [ ] Add appointment status management

### **Phase 4: Clinical Encounters (Week 4)**
- [ ] Create encounter types and interfaces
- [ ] Implement encounters API integration
- [ ] Build encounter list component
- [ ] Create encounter form
- [ ] Implement clinical notes editor
- [ ] Build vitals recording form
- [ ] Create prescription management
- [ ] Add diagnosis management
- [ ] Implement treatment plans
- [ ] Build encounter history view

### **Phase 5: Billing & Payments (Week 5)**
- [ ] Create billing types and interfaces
- [ ] Implement billing API integration
- [ ] Build invoice list component
- [ ] Create invoice generation form
- [ ] Implement payment processing
- [ ] Add payment history view
- [ ] Create payment plans management
- [ ] Implement insurance claims
- [ ] Build revenue analytics dashboard
- [ ] Add refund processing

### **Phase 6: Laboratory Management (Week 6)**
- [ ] Create laboratory types and interfaces
- [ ] Implement laboratory API integration
- [ ] Build lab tests catalog
- [ ] Create lab order form
- [ ] Implement lab results entry
- [ ] Build lab reports viewer
- [ ] Add lab test tracking
- [ ] Create quality control management
- [ ] Implement lab analytics
- [ ] Add lab test scheduling

### **Phase 7: Inventory Management (Week 7)**
- [ ] Create inventory types and interfaces
- [ ] Implement inventory API integration
- [ ] Build medicine database
- [ ] Create stock management interface
- [ ] Implement purchase order system
- [ ] Add stock alerts management
- [ ] Create supplier management
- [ ] Build inventory analytics
- [ ] Implement expiry tracking
- [ ] Add inventory reports

### **Phase 8: Analytics & Dashboard (Week 8)**
- [ ] Implement analytics API integration
- [ ] Build main dashboard
- [ ] Create patient analytics charts
- [ ] Implement revenue analytics
- [ ] Add clinic performance metrics
- [ ] Create doctor performance dashboard
- [ ] Build real-time monitoring
- [ ] Implement custom reports
- [ ] Add data visualization
- [ ] Create export functionality

### **Phase 9: Additional Features (Week 9)**
- [ ] Implement telemedicine integration
- [ ] Build patient portal features
- [ ] Add notification system
- [ ] Create compliance tracking
- [ ] Implement audit logging
- [ ] Add user management
- [ ] Create settings page
- [ ] Build profile management
- [ ] Implement role-based access
- [ ] Add activity logging

### **Phase 10: Testing & Deployment (Week 10)**
- [ ] Write unit tests for components
- [ ] Implement integration tests
- [ ] Perform end-to-end testing
- [ ] Optimize performance
- [ ] Implement error handling
- [ ] Add loading states
- [ ] Create error boundaries
- [ ] Setup production build
- [ ] Configure deployment
- [ ] Deploy to production

## 🎯 Key Features to Implement

### **1. Authentication & Authorization**
- JWT-based authentication
- Token refresh mechanism
- Role-based access control
- Session management
- Password reset functionality
- Two-factor authentication (optional)

### **2. Patient Management**
- Comprehensive patient profiles
- Medical history tracking
- Condition and allergy management
- Patient search and filtering
- Bulk import/export
- Duplicate detection
- Patient portal access

### **3. Appointment Scheduling**
- Calendar view
- Doctor availability checking
- Appointment booking
- Rescheduling and cancellation
- Automated reminders (SMS/Email)
- Waitlist management
- Recurring appointments

### **4. Clinical Documentation**
- Encounter management
- SOAP notes
- Vital signs recording
- Prescription management
- Diagnosis tracking
- Treatment plans
- Clinical templates

### **5. Billing & Payments**
- Invoice generation
- Payment processing
- Insurance claims
- Payment plans
- Revenue tracking
- Refund management
- Financial reports

### **6. Laboratory Management**
- Lab test catalog
- Order management
- Results entry
- Report generation
- Quality control
- Test tracking
- Lab analytics

### **7. Inventory Management**
- Medicine database
- Stock tracking
- Purchase orders
- Supplier management
- Expiry alerts
- Stock reports
- Inventory analytics

### **8. Analytics & Reporting**
- Real-time dashboards
- Patient analytics
- Revenue analytics
- Performance metrics
- Custom reports
- Data visualization
- Export functionality

### **9. Telemedicine**
- Video consultations
- Session management
- Recording functionality
- Device integration
- Remote monitoring
- Virtual waiting room

### **10. Notifications**
- SMS notifications
- Email notifications
- In-app notifications
- Appointment reminders
- Alert system
- Template management

## 🔒 Security Best Practices

1. **Authentication**
   - Secure JWT token storage
   - Token expiration handling
   - Refresh token rotation
   - Logout on all devices

2. **API Security**
   - HTTPS only in production
   - CORS configuration
   - Rate limiting
   - Input validation

3. **Data Protection**
   - Sensitive data encryption
   - HIPAA compliance
   - Audit logging
   - Access control

4. **Error Handling**
   - Graceful error handling
   - User-friendly error messages
   - Error logging
   - Error boundaries

## 📱 Responsive Design

- Mobile-first approach
- Tablet optimization
- Desktop layout
- Touch-friendly interfaces
- Adaptive navigation
- Responsive tables
- Mobile-optimized forms

## 🎨 UI/UX Guidelines

1. **Design Principles**
   - Clean and modern interface
   - Consistent color scheme
   - Intuitive navigation
   - Clear typography
   - Proper spacing

2. **User Experience**
   - Fast loading times
   - Smooth transitions
   - Loading indicators
   - Success/error feedback
   - Confirmation dialogs

3. **Accessibility**
   - WCAG 2.1 compliance
   - Keyboard navigation
   - Screen reader support
   - Color contrast
   - Alt text for images

## 🚀 Performance Optimization

1. **Code Splitting**
   - Route-based splitting
   - Component lazy loading
   - Dynamic imports

2. **Caching**
   - API response caching
   - LocalStorage usage
   - Service worker (optional)

3. **Optimization**
   - Image optimization
   - Bundle size reduction
   - Minification
   - Tree shaking

## 📚 Documentation Requirements

1. **Code Documentation**
   - Component documentation
   - API documentation
   - Type definitions
   - Inline comments

2. **User Documentation**
   - User guide
   - Admin guide
   - API integration guide
   - Troubleshooting guide

## 🎉 Success Criteria

- [ ] All 14 modules implemented
- [ ] 95+ API endpoints integrated
- [ ] Responsive design (mobile, tablet, desktop)
- [ ] <2s page load time
- [ ] 95%+ test coverage
- [ ] WCAG 2.1 AA compliance
- [ ] Production-ready deployment
- [ ] Complete documentation

---

## 📞 Support & Resources

- **Backend API Documentation**: See `API_DOCUMENTATION.md`
- **API Specification**: See `API_SPECIFICATION.md`
- **Core APIs Reference**: See `CORE_APIS_QUICK_REFERENCE.md`

---

**HMS React Frontend Development Prompt v1.0.0**  
*Complete guide for building a modern healthcare management system frontend*

